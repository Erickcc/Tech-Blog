{{!-- Current page title card located right under the navbar --}}
<div class="title-container">
  <div class="title-tag">
    Your Dashboard
  </div>
</div>

{{!-- Create new post menu --}}
<div class="card-element">
  <div class="card-header edit-header">
    Create New Post
  </div>
  <div class="card-content">

    <form class="form-body comment-form">
      <label for="commentBox" class="edit-inner-header">Title</label>
      <textarea class="form-control" id="title-box" rows="1"></textarea>

      <label for="commentBox" class="edit-inner-header">Content</label>
      <textarea class="form-control" id="content-box" rows="3"></textarea>
      <div class="d-grid gap-2">
        <button type="submit" class="btn btn-secondary mt-3">Create</button>
      </div>
    </form>
  </div>
</div>

{{!-- Script that listens to the create button and makes the api call --}}
<script src="/js/createPost.js"></script>